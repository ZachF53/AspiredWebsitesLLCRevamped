"""
PLANNED phase 1 for `admin_dashboard` - drop legacy owner FK columns.

NOT in the migration graph. Read clients/migrations_planned/README.md.
Run every app's phase 1 before clients phase 2.
"""

from django.db import migrations


class Migration(migrations.Migration):

    dependencies = [
        # Re-pointed at the current leaf. 0005 added the AI Employees
        # models, 0006 seeded the Prospect agent, 0007 added the run
        # message history — so 0004 is long past being the tip. Promoting
        # this against a stale parent would branch the migration graph.
        ('admin_dashboard', '0007_aiemployeerun_message_history'),
    ]

    operations = [
        migrations.RemoveField(model_name='aiassistantlog', name='client'),  # -> ClientProfile
        migrations.RemoveField(model_name='deploymentlog', name='client'),  # -> ClientProfile
    ]
